// ejercicio4.js
let numeros = [2, 7, 10, 15, 20];

for (let i = 0; i < numeros.length; i++) {
  if (numeros[i] % 2 === 0) {
    console.log(numeros[i] + " es un numero par");
  } else {
    console.log(numeros[i] + " es un numero impar");
  }
}
